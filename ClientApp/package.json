{
  "name": "visor-turnos-client",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && tsc && node scripts/copy-signalr.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "dependencies": { "@microsoft/signalr": "10.0.0" },
  "devDependencies": { "typescript": "5.9.3" }
}

# Registro de decisiones

Ultima revision tecnica: 2026-09-18.

## Decisiones implementadas

| Fecha | Decision | Responsable | Estado |
| --- | --- | --- | --- |
| 2026-09-18 | El disparador del visor es la aparición de un `am_consulta.numcon` nuevo, relacionado con la cita por `invnum`; no exige que `citas.prfnum` exista o coincida en el instante del llamado. LOLCLI crea el valor autoritativo `citas.prfnum` al usar **Crear prefactura** y aceptar, pero las filas observadas `stacon=P`/`statte=N`/`prfnum=0` demuestran que no es requisito universal del guardado. `P/S` confirma el cierre de la cita; `P` aislado no se clasifica como cerrado. | Responsable del proyecto, confirmación directa y muestra de solo lectura | Aplicada |
| 2026-09-18 | Cada llamado es autorizado por una apertura distinta de acto médico. `citas.invnum` identifica la cita y `am_consulta.numcon` identifica el intento. Solo el último `numcon`, elegido por `feccon DESC, numcon DESC`, determina la habilitación; puede prevalecer sobre un `citas.statte=S` histórico mientras no esté cerrado. El mismo `numcon` se consume una sola vez. | Responsable del proyecto, explicación directa y evidencia de producción anonimizada | Aplicada |
| 2026-09-18 | `P/S` libera el consultorio, pero nunca llama automáticamente al siguiente. El médico debe seleccionar al siguiente paciente en LOLCLI y generar su propio `numcon`. Para un reintento se crea otro `numcon`; el visor conserva el contador por `invnum` y admite como máximo cuatro `numcon` consumidos. | Responsable del proyecto, explicación directa | Aplicada |
| 2026-09-18 | El simulador DevelopmentSnapshot permite agregar citas ficticias solo en LocalDB. Cada una usa un profesional existente de la copia, nombre sintético, `statte=N`, `prfnum=0`, sin `am_consulta` y una observación de prueba que representa normal, amanecida de mañana (`A`), amanecida de tarde (`A1`) o EMA. El flujo posterior conserva las mismas reglas del visor. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-18 | `am_consulta.stacon=T` significa acto médico abierto y `stacon=P` acto guardado/cerrado. Una cita solo se habilita con prefactura válida y cuando su último `numcon` está en `T`; `statte=N` no es obligatorio porque una reapertura observada conservó temporalmente `statte=S`. La mera existencia histórica de cualquier `T` no habilita el llamado. | Responsable del proyecto, confirmación directa y muestra anonimizada | Reemplazada por la regla `numcon` del 2026-09-18 |
| 2026-09-18 | Prioridad clínica validada para la cola de citas: 1) EMA (`obscit=EMA`) habilitado puede ser llamado en cualquier momento de la jornada, incluso antes de su hora programada; 2) amanecida identificada por `obscit` de una letra (`A`, `B`, `C`, `D`, etc.) o letra más `1` para la tarde (`A1`, `B1`, `C1`, etc.); 3) las demás observaciones. EMA y amanecida requieren un `numcon` nuevo no cerrado. Un llamado ya iniciado nunca se interrumpe. | Responsable del proyecto, confirmación directa | Aplicada |
| 2026-09-18 | Un paciente puede consumir como máximo cuatro `numcon` distintos. Cada intento dura hasta 60 segundos, con un aviso inicial y una repetición a los 30 segundos. El mismo `numcon` nunca inicia otro ciclo; un quinto `numcon` queda excluido de nuevas selecciones. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-18 | Al cumplirse el minuto se detienen banner y voz, pero un acto cuyo último `numcon` no está cerrado mantiene ocupado solo su consultorio hasta aparecer `P/S`. No existe reencolado automático. Un nuevo intento requiere que el médico guarde el acto y posteriormente cree otro `numcon`, antes de las 12:00 en la mañana o de las 17:30 en la tarde. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-18 | La TV y su altavoz procesan un único llamado activo globalmente. Aunque varios consultorios tengan cabeceras habilitadas, la cola selecciona una sola según prioridad y orden; ningún consultorio puede interrumpir el llamado, repetición o tolerancia vigente de otro. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-18 | La pantalla pública usa únicamente las etiquetas de estado `Llamando` y `Próximo`. Los detalles de prefactura, acto médico, intentos y ausencias se reservan para `/simulador`, exclusivamente en DevelopmentSnapshot. El simulador expone la cola interna en memoria y se refresca cada 3 segundos. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-18 | Los anuncios de la TV se reproducen únicamente con una voz instalada cuyo idioma sea español (`es-*`). Se priorizan voces locales clásicas (`Microsoft Sabina`, luego `Microsoft Helena`) por su comportamiento fonético estable; si no existe voz española, se omite el audio. El texto hablado se normaliza a minúsculas y admite reemplazos fonéticos sin alterar el nombre visible, por ejemplo `Jhonatan`/`Jonathan` → `Yonatán`, `Rogel` → `Rojél` y `Miraval` → `Miravál`. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-18 | Se refina la presentación para televisión: el encabezado usa un degradado institucional de rojo oscuro a rojo claro; fuera de las franjas configuradas no se muestra rótulo horario; el banner reserva dos tercios para el paciente y un tercio para consultorio y médico, ambos con mayor jerarquía; en la lista por consultorio solo el paciente llamado usa negrita; y la cola general asigna anchos diferenciados a orden, paciente, destino y estado. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-16 | Cada llamado anuncia al paciente inicialmente y repite el aviso a los 30 segundos. A los 60 segundos, si no existe cambio de cierre en LOLCLI, desaparece del llamado, se reubica al final de la cola de su consultorio/médico y puede volver a ser llamado hasta cuatro veces. Después del cuarto intento el visor no escribe ni elimina la cita; esa acción corresponde a LOLCLI. | Responsable del proyecto, solicitud directa | Reemplazada por la regla de prefactura y `am_consulta` del 2026-09-17 |
| 2026-09-16 | Se autoriza que el visor público anuncie por voz el nombre visible del paciente y el consultorio al inicio de cada llamado y una vez más a los 30 segundos. | Responsable del proyecto | Aplicada |
| 2026-09-16 | Razor Pages + TypeScript + SignalR; sin Blazor. | Lineamiento del proyecto | Aplicada |
| 2026-09-16 | La aplicacion usa exclusivamente el DSN ODBC `LOLCLI9000`, independientemente del perfil de inicio. Cuando ODBC no esta disponible, conserva el ultimo snapshot real o informa indisponibilidad; no existen datos demo como alternativa. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-16 | La cola consulta solo la jornada actual de Cuajone. Las listas públicas muestran citas pendientes desde la hora actual en adelante; una cita anterior solo permanece visible si ya fue activada como llamado. Cada consultorio inicia automáticamente un turno solo durante el mismo minuto de `citdat` y continúa con el siguiente elegible de su propia cola; no inicia llamadas para minutos anteriores. `cithll` no bloquea el llamado porque nunca se llena. Los casos cerrados o con prefactura se excluyen de la interfaz pública. | Responsable del proyecto, solicitud directa | Reemplazada por la regla de cola interna del 2026-09-17 |
| 2026-09-17 | La cola interna conserva todas las citas abiertas de cada consultorio/médico en orden de prioridad aprobada y `citdat`; así se conoce la cabecera antes del llamado. El anuncio por voz solo se habilita cuando esa cabecera tiene `citas.prfnum <> 0` y existe su registro correspondiente en `am_consulta` por `prfnum`. El sondeo ODBC central sincroniza la cola cada 3 segundos. `stacon`, `feccon`, `feccre` y `fecumv` se conservan internamente, pero no determinan todavía el cierre o el llamado porque su semántica no está validada. | Responsable del proyecto, solicitud directa | Reemplazada por la confirmación de `T`/`P` del 2026-09-18 |
| 2026-09-17 | Se autoriza un snapshot local de Development en SQL Server LocalDB con las tablas mínimas de la consulta diaria: `citas`, `medicos`, `consultorios` y `am_consulta`. LOLCLI permanece estrictamente de solo lectura. El uso del snapshot exige entorno `Development` y `DataSource:EnableDevelopmentSnapshot=true`; no se activa por defecto ni puede usarse en Production. | Responsable del proyecto, autorización expresa | Aplicada |
| 2026-09-17 | La jornada visible se divide por `citdat` en dos ventanas: `[00:00, 12:00)` y `[12:00, fin del día)`. Antes de las 12:00 el visor consulta solo la primera; desde las 12:00 consulta solo la segunda. `Queue:SessionSplitHour=12` deja el corte explícito y configurable. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-17 | Se autoriza `/simulador` exclusivamente con `DevelopmentSnapshot` habilitado. **Llamar** crea un `numcon`; **Crear prefactura** es opcional y actualiza solo `citas.prfnum`; **Guardar** actualiza el último acto a `P` y cambia `statte=S` para probar el flujo `P/S`. Un nuevo **Llamar** crea otro `numcon` y restablece `statte=N` en la copia local. Sus endpoints escriben únicamente en LocalDB. | Responsable del proyecto, solicitud directa; refinada con evidencia del 2026-09-18 | Aplicada |
| 2026-09-17 | El simulador local muestra las columnas técnicas `mednam`, `Consultorio`, `pacnam`, `citdat`, `statte`, `obscit`, `prfnum`, `invnum`, `numcon`, `stacon`, `feccon`, `feccre` y `fecumv`, incluso para citas locales cerradas. Es una vista de prueba aislada en LocalDB; esos campos no se incorporan a la pantalla pública. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-17 | El simulador local tiene dos vistas: detalle técnico filtrable por consultorio y próximos por orden de la cola interna. La vista de orden excluye las citas cerradas e indica si el turno tiene un `numcon` habilitante, para que la simulación no confunda una cita programada con un llamado posible. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-17 | La cabecera pública muestra el bloque horario vigente: turno mañana de 07:00 a 11:59, receso de 12:00 a 13:59, turno tarde de 14:00 a 17:59 y fuera de horario desde las 18:00 hasta las 06:59. Los límites viven en la sección configurable `Schedule`. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-17 | Durante un llamado activo, el panel rotativo de consultorios salta al consultorio/médico que está llamando y pausa su rotación hasta terminar el llamado. El banner mantiene un verde menta claro fijo para diferenciarse del encabezado rojo; el pálpito modifica únicamente su sombra exterior, sin cambiar color, brillo ni escala. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-17 | La cola general se presenta como cinco tarjetas numeradas. El llamado activo se fija primero con indicador verde y `Pase al consultorio`; los demás usan indicador naranja. Como la consulta no entrega una especialidad independiente, las tres líneas informativas muestran profesional, consultorio y hora programada sin inventar datos. | Responsable del proyecto, referencia visual solicitada | Reemplazada: se restauró el diseño de filas anterior |
| 2026-09-17 | Se restaura el diseño anterior de filas del panel general y se conserva únicamente la columna nueva `Orden`. El llamado activo continúa destacado en el banner y en el panel de su consultorio. | Responsable del proyecto, solicitud directa | Reemplazada por la dirección visual Pro+ |
| 2026-09-17 | Se adopta la dirección visual Pro+ para televisión: fondo neutro, encabezado institucional moderado, relación de columnas aproximada 30/70 y turnos presentados como tarjetas suaves en ambos paneles. El llamado activo aparece primero en la cola general y se distingue por texto, icono, fondo menta y borde verde; el modelo no inventa una especialidad distinta del consultorio confirmado. | Responsable del proyecto, especificación visual directa | Aplicada |
| 2026-09-16 | El repositorio ODBC no selecciona `pacnam` ni `pachis`. | Privacidad por diseño | Reemplazada: `pacnam` autorizado como identificador publico; `pachis` sigue prohibido |
| 2026-09-16 | Sin identificador publico aprobado, una fila real no se publica. | Privacidad por diseño | Aplicada |
| 2026-09-16 | En la cola de citas, `citas.obscit = EMA` identifica examen medico y recibe `PriorityTier=1`. La observacion cruda no se publica ni registra. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-16 | La pantalla tiene un panel rotativo agrupado por consultorio y medico, y un panel general fijo ordenado por prioridad que muestra ambos datos de destino. | Responsable del proyecto, solicitud directa | Aplicada |
| 2026-09-16 | Se activa el DSN ODBC `LOLCLI9000` en modo lectura para `siscod=1` (Cuajone), con sondeo central cada 3 segundos, limite de 100 filas y timeout de 5 segundos. | Responsable del proyecto, autorizacion expresa | Aplicada |
| 2026-09-16 | El responsable autoriza mostrar `citas.pacnam` como identificador publico porque el paciente no conoce `invnum`; el visor no muestra `pachis`, `obscit` crudo ni `prfnum`. | Responsable del proyecto, autorizacion expresa | Aplicada |

## Pendientes de aprobacion

No se ha cerrado ninguno por inferencia. Registrar fecha, responsable y evidencia cuando se responda cada punto.

1. Valores reales de `statte` y transiciones.
2. Ausencia de prefactura: `NULL`, `0` o ambos.
3. Campo y valores para amanecida.
4. Integracion o separacion de emergencias y su precedencia exacta respecto a EMA.
5. Campo y regla de atencion preferencial.
6. Minutos y efecto de tolerancia.
7. Identificador publico conocido por el paciente.
8. Politica de nombre anonimizado.
9. Permanencia de casos llamados o cerrados.
10. Intervalo y SLA de actualizacion.
11. `siscod` por sede.
12. Version LTS aprobada por TI (el proyecto conserva `net10.0` del repositorio recibido).
13. Controlador y DSN ODBC.
14. Topologia IIS-SQL e identidad efectiva.
15. Cuenta personal temporal o cuenta de servicio/gMSA.
16. Texto, sonido y volumen.
17. Horario operativo y cambio de dia.

## Nota sobre demostracion

Los identificadores `DEMO-*`, estados y consultorios del modo Development son datos ficticios de presentacion. No representan codigos de LOLCLI ni decisiones del negocio.
